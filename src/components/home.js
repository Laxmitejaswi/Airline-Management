import React from 'react';
import Navbar from './Navbar';
import FlightBooking from './FlightBooking';

const Home = () => {
    return (
        <div>
            <Navbar />
            <FlightBooking />
        </div>
    );
};

export default Home;
